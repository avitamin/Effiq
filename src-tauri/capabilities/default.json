{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for typed Effiq review runner commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
